import * as THREE from "three";

const assignPlanetCharacteristics = (
  rotationSpeed,
  revolutionSpeed,
  size,
  distanceFromAxis,
) => {
  return { rotationSpeed, revolutionSpeed, size, distanceFromAxis };
};
